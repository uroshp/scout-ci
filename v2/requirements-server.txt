# Runtime deps for the Flask viewer (server.py) — the de-Streamlit'd app for Cloud Run.
# Profiled minimal set: server.py reuses scout.page/display/selfserve/config/store, which pull
# in ONLY these at runtime. Deliberately excludes streamlit, anthropic, claude-agent-sdk, pypdf,
# beautifulsoup4, rapidfuzz (all engine-only) — keeping the image small.
flask==3.1.3
gunicorn==26.0.0
httpx==0.28.1
python-dotenv==1.2.2
jsonschema==4.26.0
