{
  "name": "Python 3 + Claude Code",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bookworm",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "20"
    }
  },
  "customizations": {
    "codespaces": {
    
    },
    "vscode": {
      "settings": {},
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "updateContentCommand": "[ -f packages.txt ] && sudo apt-get update && sudo xargs apt-get install -y <packages.txt; [ -f v2/requirements.txt ] && pip3 install --user -r v2/requirements.txt; pip3 install --user streamlit; npm install -g @anthropic-ai/claude-code; echo '✅ Environment ready'",
  "postAttachCommand": "tmux new -A -s claude || true",
  "portsAttributes": {
    "8501": {
      "label": "Application",
      "onAutoForward": "openPreview"
    }
  },
  "forwardPorts": [
    8501
  ]
}
